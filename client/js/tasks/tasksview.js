const React = require("react");

const TasksView = React.createClass({
    render() {
        return (
            <div>
                <h1>tasks</h1>
            </div>
        );
    }
});

module.exports = TasksView;
